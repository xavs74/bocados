// Cloudflare Workers configuration. Bocados is a static site: `npm run build`
// writes it to dist/ and Cloudflare serves those files as they are, with no
// Worker script. Routing uses #/ hashes, so no fallback rules are needed.
{
  "name": "bocados",
  "compatibility_date": "2026-09-17",
  "assets": {
    "directory": "./dist"
  },
  // bocados.org is registered with Cloudflare, so deploying attaches it here.
  "routes": [{ "pattern": "bocados.org", "custom_domain": true }]
}
